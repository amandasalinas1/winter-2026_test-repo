---
title: "test-doc"
author: "Amanda Salinas"
format: html
editor: visual
date: "2026-02-18"
---

## 

```{r}
#| label: set up chunk
#| message: false
library(tidyverse) # general use for data wrangling, cleaning, etc.
library(lterdatasampler) # package with lter data sets

data(pie_crab) # extracting pie crab data set from lter data sampler
```

# Exploratory visualization

```{r}
#| label: latitude size plot
ggplot(data = pie_crab, # pie data set to plot
       aes(x = latitude, # x-axis is latitude
           y = size, # y-axis is crab size
           color = site)) + # color by site
  geom_point() + # creating a scatter plot
  theme_minimal()  #changing theme
  

```
